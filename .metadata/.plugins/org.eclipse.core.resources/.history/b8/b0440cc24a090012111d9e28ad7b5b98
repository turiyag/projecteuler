public class TestMain {
	
	/**
	 * @param args
	 */
	public static void main(final String[] args) {
		final PrimeClient pc = new PrimeClient(10);
		for (int i = 0; i < pc.GetCount(); i++) {
			System.out.println(pc.GetPrime(i));
		}
	}
	
}
